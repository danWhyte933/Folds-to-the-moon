public class PaperFolder {
  public static Long fold(Double distance) {
    if (distance < 0) {return null; }
   double thickness = 0.0001;
   int count = 0; 
   for (int i = 0; thickness < distance; i++) {
   thickness = thickness*2;
   count++;  
   } Long l= new Long(count);
    return l;
  }
}
